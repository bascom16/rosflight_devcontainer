# ROS 2 build artifacts
/build/
/install/
/log/

# ROSflight source repositories — cloned by scripts/setup_workspace.sh.
# They are kept out of this repo so it stays a lightweight, reproducible
# devcontainer template. Run the setup script (or start the container) to
# populate them.
/src/rosflight_ros_pkgs/
/src/rosplane/
/src/roscopter/

# Claude Code local (user-specific) settings
.claude/settings.local.json

# Editor / OS cruft
*.swp
*.swo
*~
.DS_Store
.idea/

# SIL firmware parameter storage (written by sil_board)
rosflight_memory/
